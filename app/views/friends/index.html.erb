<% @friends.each do |f| %>
  <div class="card-product">
   <%= cl_image_tag f.pictures.first.photo , class: "avatar-large", alt: "avatar-large"%>
    <div class="card-product-infos">
      <h2><%=f.name %></h2>

    </div>
  </div>
<% end %>
